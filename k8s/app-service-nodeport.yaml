apiVersion: v1
kind: Service
metadata:
  name: task-runner-api
  namespace: task-runner
spec:
  type: NodePort
  selector:
    app: task-runner-api
  ports:
    - name: http
      port: 80
      targetPort: 8081
      nodePort: 30080
